#include <stdio.h>
#include <string.h>

void get_extension(const char *filename, char *extension);

int
main (int argc, char *argv[])
{
	
	return 0;
}
	
void
get_extension(const char *filename, char *extension)
{
	int len;
	
	len = strlen(filename);

	while (*p++ != '.')
	{
		;
	}
	strcpy(extension, p);
	
}
